package com.sellandrent.service_tier;

import com.sellandrent.model_tier.Rent;
import com.sellandrent.repository_tier.Rent_Repository;
import com.sellandrent.DatabaseConnectionTest;

import java.sql.Connection;
import java.sql.SQLException;
import java.util.ArrayList;
import java.util.List;

public class RentService {
    private Rent_Repository rent_repository;
    private Connection connection;

    // Bağlantıyı her işlem için açan yardımcı metot
    private Connection getConnection() throws SQLException {
        if (connection == null || connection.isClosed()) {
            connection = DatabaseConnectionTest.createConnection();
        }
        return connection;
    }

    private void close(AutoCloseable closeable) {
        if (closeable != null) {
            try {
                closeable.close();
            } catch (Exception e) {
                e.printStackTrace();
                // Loglama eklenebilir
            }
        }
    }

    public Rent getRentById(int id) {
        Connection conn = null;
        Rent_Repository repo = null;
        try {
            conn = getConnection();
            repo = new Rent_Repository(conn);
            return repo.getRentById(id);
        } catch (SQLException e) {
            e.printStackTrace();
            return null;
        } finally {
            close(conn);
        }
    }

    public List<Rent> getAllRents() {
        Connection conn = null;
        Rent_Repository repo = null;
        try {
            conn = getConnection();
            repo = new Rent_Repository(conn);
            return repo.getAllRents();
        } catch (SQLException e) {
            e.printStackTrace();
            return null;
        } finally {
            close(conn);
        }
    }

    public void addRent(Rent rent) {
        Connection conn = null;
        Rent_Repository repo = null;
        try {
            conn = getConnection();
            repo = new Rent_Repository(conn);
            repo.addRent(rent);
        } catch (SQLException e) {
            e.printStackTrace();
        } finally {
            close(conn);
        }
    }

    public void updateRent(Rent rent) {
        Connection conn = null;
        Rent_Repository repo = null;
        try {
            conn = getConnection();
            repo = new Rent_Repository(conn);
            repo.updateRent(rent);
        } catch (SQLException e) {
            e.printStackTrace();
        } finally {
            close(conn);
        }
    }

    public void deleteRent(int id) {
        Connection conn = null;
        Rent_Repository repo = null;
        try {
            conn = getConnection();
            repo = new Rent_Repository(conn);
            repo.deleteRent(id);
        } catch (SQLException e) {
            e.printStackTrace();
        } finally {
            close(conn);
        }
    }

    public List<Rent> getRentsByCustomerId(int customerId) {
        Connection conn = null;
        Rent_Repository repo = null;
        try {
            conn = getConnection();
            repo = new Rent_Repository(conn);
            return repo.getRentsByCustomerId(customerId);
        } catch (SQLException e) {
            e.printStackTrace();
            return new ArrayList<>();
        } finally {
            close(conn);
        }
    }
}